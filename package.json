{
  "name": "pdf-ephemeral",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:backend": "bun --bun run workspace backend start:dev",
    "dev:frontend": "bun --bun run workspace frontend dev",
    "dev": "bun --bun run dev:backend & bun --bun run dev:frontend"
  }
}
